# Treat the VHS tape source as documentation (it exists only to regenerate
# the assets; the project runs without it).
assets/demo.tape linguist-documentation

# Keep binary assets excluded from linguist analysis explicitly.
assets/* binary
*.svg binary
*.gif binary
